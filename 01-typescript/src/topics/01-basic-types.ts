const name: string = 'Strider';
let full: number | string = 2;
let isAlive: boolean = true;
let variable: number | 'FULL';

variable = 'FULL'

console.log({
    name, full, isAlive, variable
})

export {}